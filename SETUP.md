# Profile README

This profile belongs to the GitHub account `Esen-wyz`.

## Publishing

Place `README.md` and `assets/hero.png` on the default branch of the public repository `Esen-wyz/Esen-wyz`. GitHub displays the root README on the account's profile. See [GitHub's profile README documentation](https://docs.github.com/en/account-and-profile/how-tos/profile-customization/managing-your-profile-readme).

Update the profile files while preserving any unrelated repository content.

## Files

- `README.md`: the profile text and hero image reference.
- `assets/hero.png`: the hero image, combining a ground–aerial observation scene with introductory typography.
- `docs/hero-prompt.md`: the image generation prompt and provenance notes.
- `preview.html`: a local preview of the README. GitHub's own Markdown rendering determines the final appearance.
- `build_preview.py`: the preview generator. Run `python build_preview.py` with the Python `markdown` package installed.

The hero uses an AI-generated conceptual scene. It illustrates the research direction and does not depict a real dataset, an implemented method, or experimental results. It is a static image with no buttons or external image service dependency.

## Editorial guidance

Use English for headings, descriptions, image alternative text, and supporting copy. Only the research direction line beneath the account name may remain in Chinese.

Keep the profile concise and professional. The research description reflects the stated computer vision and ground–aerial gait recognition focus. Add affiliations, publications, performance figures, or contact details only when verified. Add project links when the work is ready for public presentation.
